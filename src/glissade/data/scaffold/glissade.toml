# Optional project defaults. Delete this file if you don't need it.
[glissade]
# deck = "welcome"   # which deck `glissade start` opens
# port = 8000
# host = "0.0.0.0"   # 0.0.0.0 lets a phone on the same Wi-Fi reach the remote
